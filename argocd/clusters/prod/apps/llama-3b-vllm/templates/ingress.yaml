apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: llama-3b-vllm
  annotations:
    kubernetes.io/ingress.class: "traefik"
    cert-manager.io/cluster-issuer: "cloudflare-prod"
spec:
  tls:
  - hosts:
    - {{ .Values.ingress.host | quote }}
    secretName: llama-3b-vllm-tls
  rules:
  - host: {{ .Values.ingress.host | quote }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: llama-3b-vllm
            port:
              number: 80
